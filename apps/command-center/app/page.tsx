import { PlaybookDashboard } from "@/components/playbook/PlaybookDashboard";

// First logged-in view: the playbook-first Option C implementation workspace.
export default function HomePage() {
  return <PlaybookDashboard />;
}
